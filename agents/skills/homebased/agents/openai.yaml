interface:
  display_name: "Homebased"
  short_description: "Run background agent and general task workloads that report back to this thread"
  default_prompt: "Use $homebased to run this work as a background task and report back here when it finishes."
