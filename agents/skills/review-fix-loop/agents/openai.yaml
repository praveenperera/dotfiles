interface:
  display_name: "Review Fix Loop"
  short_description: "Run bounded local review and Luna Max fix loops"
  default_prompt: "Use $review-fix-loop to run a bounded review and local Luna Max fix loop for the current branch, and request separate authorization before any publication or PR mutation."
