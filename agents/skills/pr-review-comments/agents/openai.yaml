interface:
  display_name: "PR Review Comments"
  short_description: "Inspect thread-aware PR feedback with prc"
  default_prompt: "Use $pr-review-comments to fetch and summarize PR review feedback."
policy:
  allow_implicit_invocation: false
